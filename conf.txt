/* Configuration file to define all arguments. Note it is
totally safe to write in the middle of the file, though all
parameters must be supplied in the following format in order
for config. parse to work. The format is:
<Param. name> : <value>
(where the two spaces and the end of line are compulsory!)
Also note the order for the parameters is irrelevant.*/
// F defines rows
F : 10
// K defines number of matches
K : 9
// C defines columns
C : 5

